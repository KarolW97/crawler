version: "3"
services:
  app:
    restart: always
    build: . # Current directory!
    command: "python3 manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      - CRAWLER_DB=crawler
      - HOST=10.95.2.31